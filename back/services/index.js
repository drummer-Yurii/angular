const all = './all/';

const self = {
    // ui: require(`${all}ui.service.js`),
    // app: require(`${all}app.service.js`),
    // msg: require(`${all}msg.service.js`),
    // map: require(`${all}map.service.js`),
    user: require(`${all}user.service.js`),
    // chat: require(`${all}chat.service.js`),
    auth: require(`${all}auth.service.js`),
    // mail: require(`${all}mail.service.js`),
    // task: require(`${all}task.service.js`),
    // room: require(`${all}room.service.js`),
    // file: require(`${all}file.service.js`),
    // hotel: require(`${all}hotel.service.js`),
    // alarm: require(`${all}alarm.service.js`),
    // crypto: require(`${all}crypto.service.js`),
    // socket: require(`${all}socket.service.js`),
    // phrase: require(`${all}phrase.service.js`),
    // speech: require(`${all}speech.service.js`),
    // visitor: require(`${all}visitor.service.js`),
    // testing: require(`${all}testing.service.js`),
    // metrics: require(`${all}metrics.service.js`),
    // language: require(`${all}language.service.js`),
    // translate: require(`${all}translate.service.js`),
    // broadcast: require(`${all}broadcast.service.js`),
    // assistant: require(`${all}assistant.service.js`),
    // advertising: require(`${all}advertising.service.js`),
    // administrators: require(`${all}administrators.service.js`),
    // assistantSocket: require(`${all}assistant-socket.service.js`),
}

module.exports = self;